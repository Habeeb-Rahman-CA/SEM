# EXAMPLE ONLY — replace every value before applying, and NEVER commit
# the real secrets. In production, source these from Sealed Secrets,
# External Secrets Operator (pointing at Vault/AWS Secrets Manager/
# GCP Secret Manager), or your cluster's secret provisioning tooling.
apiVersion: v1
kind: Secret
metadata:
  name: sem-backend-secrets
  labels:
    app.kubernetes.io/name: sem-backend
    app.kubernetes.io/part-of: sem
type: Opaque
stringData:
  DB_USERNAME: "postgres"
  DB_PASSWORD: "REPLACE_ME"
  JWT_SECRET: "REPLACE_ME_LONG_RANDOM_HEX"
  REDIS_PASSWORD: ""
  CLOUDINARY_CLOUD_NAME: "REPLACE_ME"
  CLOUDINARY_API_KEY: "REPLACE_ME"
  CLOUDINARY_API_SECRET: "REPLACE_ME"
